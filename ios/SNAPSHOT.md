# iOS source sync

Private repo updated from local `~/Downloads/JJNightBrawl` (main @ 08200a0).

## Recent commits
- Fix black screen on physical iPhone device launch
- Fix launch white screen and harden asset load for sim reliability
- iPhone 17 sim: fix HUD overlap, asset load hang, auto-start hook
- iOS port with freeze/white-screen fixes applied

## Note
PNG assets remain primarily in local project / Desktop tar backup.
Run `gh auth login` then `git push -u origin main` from Downloads for full binary push.
